---
'@delfini/cli': patch
'@delfini/drift-engine': patch
'@delfini/action-core': patch
---

docs: rewrite the package READMEs (npm package pages) to clearly explain what each package does, for developers and teams evaluating Delfini. Drops internal requirement-code jargon and contributor-only procedure from the published READMEs. This patch republishes all three packages so the updated descriptions reach npm, and carries the repository metadata + provenance onto drift-engine and action-core.
